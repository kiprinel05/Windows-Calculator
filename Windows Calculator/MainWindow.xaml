<Window x:Class="WPFCalculator.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Calculator" Height="500" Width="350" ResizeMode="NoResize">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Operator Display -->
        <TextBlock x:Name="txtOperator" FontSize="20" TextAlignment="Right"
                   HorizontalAlignment="Stretch" VerticalAlignment="Center"
                   Margin="10" Foreground="Gray" />

        <!-- Main Display -->
        <TextBox x:Name="txtDisplay" FontSize="40" TextAlignment="Right"
                 HorizontalAlignment="Stretch" VerticalAlignment="Center"
                 Margin="10" IsReadOnly="True" Text="0"/>

        <!-- Grid pentru butoane -->
        <Grid x:Name="ButtonGrid" Grid.Row="2" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>

                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>

            </Grid.ColumnDefinitions>
        </Grid>

    </Grid>
    
</Window>
